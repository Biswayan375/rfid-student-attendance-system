<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | RFIDAMS</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <style>
        .formContainer {
            width: 100vw;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .formContainer > form {
            width: 30%;
            border-radius: 13px;
            box-shadow: 0px 0px 15px 0px #a9a8a8;
        }

        .formContainer > form > .formBody { padding-left: 25px; padding-right: 25px; backdrop-filter: blur(8px); }

        .formContainer > form > .formHeading {
            background-color: blueviolet;
            border-top-left-radius: 13px;
            border-top-right-radius: 13px;
            padding: 10px;
            text-align: center;
            color: white;
        }
    </style>
</head>
<body style="background: url('./images/bgImage1.jpg'); background-size: cover; background-repeat: no-repeat;">
    <div class="formContainer">
        <form action="login" method="post">
            <div class="formHeading">
                <h4>RFID Attendance Management System</h4>
                <span style="font-size: 20px; font-weight: bold; color: #95b7ff;">Admin Login</span>
            </div>
            <div class="formBody">
                <% 
                    if (message != '' && message != null && message != undefined) {
                        %>
                        <div id="message-div">
                            <div id="info-div" class="alert alert-danger alert-dismissible fade show" role="alert">
                                <div id="info-div-content">
                                    <%= message %>
                                </div>
                                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                            </div>
                        </div>
                        <%
                    }
                %>
                <label class="form-label" for="username">Username</label>
                <input class="form-control" type="text" id="username" name="username" required>
        
                <label class="form-label" for="password">Password</label>
                <input class="form-control" type="password" id="password" name="password" required>
        
                <div style="text-align: center;">
                    <button class="btn btn-outline-success m-2">Login</button>
                </div>
            </div>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
</body>
</html>